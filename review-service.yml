server:
 port: 0
 
eureka:
 client:
  service-url:
    defaultZone: http://localhost:8761/eureka/

app:
  secret: karam is the owner + this is added on runtime

management:
  endpoints:
    web:
      exposure:
        include: "*"
