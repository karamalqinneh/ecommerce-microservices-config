server:
 port: 0
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka
  instance:
    prefer-ip-address: true

spring:
  kafka:
    bootstrap-servers:
      - localhost:29092