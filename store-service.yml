spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: store-main
  kafka:
    bootstrap-servers:
      - kafka:9092
      - kafka:9093

server:
 port: 0
 
eureka:
 client:
  service-url:
    defaultZone: http://localhost:8761/eureka/

